@page
@model projetEsport.Pages.Admin.Users.ApproveUsersModel
@{
    ViewData["Title"] = "Utilisateurs";
}

<h1>@ViewData["Title"]</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].Mail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Users)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Mail)
                </td>
                <td>
                    <form method="post">
                        <input asp-for="@item.ID" value="@item.ID" type="hidden" />
                        <input type="submit" class="btn btn-success" asp-page-handler="ApproveUser" asp-route-id="@item.ID" value="Accepter la demande" />
                        <input type="submit" class="btn btn-danger" asp-page-handler="RejectUser" asp-route-id="@item.ID" value="Rejeter la demande" />
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
